?# 新人上手路线

## 快速上手（1-2天）

### 第一步：环境搭建�?小时�?
**目标**：让项目在本地跑起来

1. **安装依赖**
   ```bash
   # Java 17
   java -version
   
   # Maven 3.8+
   mvn -version
   
   # Docker & Docker Compose
   docker --version
   docker-compose --version
   ```

2. **启动基础设施**
   ```bash
   cd knowflow
   docker-compose up -d
   
   # 检查状�?   docker-compose ps
   ```

3. **初始化数据库**
   ```bash
   # PostgreSQL 服务会通过 docker-compose 挂载自动初始�?   docker-compose up -d postgres
   ```

4. **配置API Key**
   ```bash
   # 复制配置模板
   cp knowflow-secrets.example.yaml knowflow-secrets.yaml
   
   # 编辑配置，填入智谱AI API Key
   vi knowflow-secrets.yaml
   ```

5. **构建运行**
   ```bash
   # 构建
   mvn clean package -DskipTests
   
   # 运行
   java -jar bootstrap/target/knowflow-0.0.1-SNAPSHOT.jar
   
   # 或使用脚�?   ./start-knowflow.cmd  # Windows
   ```

6. **验证**
   ```bash
   # 检查健康状�?   curl http://localhost:9090/api/knowflow/actuator/health
   
   # 应该返回：{"status":"UP"}
   ```

**常见问题**�?- �?端口被占�?�?修改 `server.port`
- �?数据库连接失�?�?检�?`docker-compose ps postgres`
- �?Redis连接失败 �?检查密码是否为 `123456`

---

### 第二步：理解项目结构�?小时�?
**目标**：知道代码在哪里

1. **阅读文档**
   - `README.md`：项目介�?   - `CLAUDE.md`：开发指�?   - `docs/wiki/01-项目概览.md`：本Wiki第一�?
2. **浏览目录**
   ```bash
   tree -L 2 -d
   ```
   
   重点关注�?   - `bootstrap/`：业务代码主目录
   - `framework/`：基础设施
   - `infra-ai/`：AI抽象�?   - `resources/database/`：数据库脚本

3. **查看核心文件**
   - `KnowFlowApplication.java`：启动类
   - `application.yaml`：配置文�?   - `RAGChatController.java`：对话API入口
   - `RAGChatServiceImpl.java`：核心流�?
---

### 第三步：跑通第一个API�?小时�?
**目标**：理解请求如何处�?
1. **创建知识�?*
   ```bash
   curl -X POST http://localhost:9090/api/knowflow/knowledge/base \
     -H "Content-Type: application/json" \
     -d '{
       "name": "测试知识�?,
       "description": "我的第一个知识库"
     }'
   
   # 记录返回�?knowledgeBaseId
   ```

2. **上传文档**
   ```bash
   curl -X POST http://localhost:9090/api/knowflow/knowledge/document \
     -F "file=@test.pdf" \
     -F "knowledgeBaseId=kb-123456"
   ```

3. **查看处理状�?*
   ```bash
   curl http://localhost:9090/api/knowflow/knowledge/document/{documentId}
   ```

4. **发起对话**
   ```bash
   curl -X POST http://localhost:9090/api/knowflow/rag/v3/chat \
     -H "Content-Type: application/json" \
     -d '{
       "question": "这个文档讲了什么？",
       "conversationId": "test-conv-001"
     }'
   ```

5. **查看日志**
   ```bash
   tail -f app.log
   ```

**调试技�?*�?- �?`RAGChatController.streamChat()` 打断�?- 单步跟踪整个流程
- 观察每个环节的输入输�?
---

### 第四步：理解核心流程�?小时�?
**目标**：知道对话是如何处理�?
**推荐阅读顺序**�?
1. **入口�?*
   - `RAGChatController.java:82`：接收请�?   - 理解 SSE（Server-Sent Events）流式响�?
2. **服务�?*
   - `RAGChatServiceImpl.java:82`：核心流程编�?   - 逐行阅读 `streamChat()` 方法
   - 理解10个步骤的作用

3. **记忆管理**
   - `ConversationMemoryService.java`：会话记�?   - 理解滑动窗口和自动总结

4. **查询改写**
   - `QueryRewriteService.java`：上下文补全
   - 理解为什么需要改�?
5. **意图识别**
   - `IntentResolver.java`：意图分�?   - 理解领域→类别→主题的树状结�?
6. **检索引�?*
   - `RetrievalEngine.java`：多通道检�?   - 理解并行检索和后处�?
7. **Prompt组装**
   - `RAGPromptService.java`：组装最终Prompt
   - 理解知识上下文如何注�?
8. **LLM调用**
   - `RoutingLLMService.java`：模型路�?   - 理解故障转移机制

**画流程图**�?- 自己画一遍完整流程图
- 标注每个环节的输入输�?- 标注数据存储位置（Redis/PostgreSQL/Milvus�?
---

## 深入理解�?-5天）

### 第五步：理解数据模型（半天）

**目标**：知道数据如何存�?
1. **查看数据库表**
   ```sql
   \dt  -- 列出所有表
   
   \d t_knowledge_base      -- 查看表结�?   \d t_knowledge_document
   \d t_knowledge_chunk
   ```

2. **理解实体关系**
   ```
   用户 �?知识�?�?文档 �?分块 �?向量
   ```

3. **查看实体�?*
   - `KnowledgeBaseDO.java`
   - `KnowledgeDocumentDO.java`
   - `KnowledgeChunkDO.java`

4. **理解状态流�?*
   ```
   文档状态：PENDING �?RUNNING �?COMPLETED
   ```

5. **查看Redis数据**
   ```bash
   redis-cli -a 123456
   
   # 查看所有Key
   KEYS rag:*
   
   # 查看会话记忆
   GET rag:memory:conv-123
   ```

**阅读文档**：`docs/wiki/07-关键数据结构.md`

---

### 第六步：理解文档处理流程（半天）

**目标**：知道文档如何变成向�?
1. **阅读消费者代�?*
   - `DocumentIngestionConsumer.java`：MQ消费�?   - 理解异步处理机制

2. **理解解析�?*
   - `DocumentParser.java`：Tika解析
   - 支持的文件格�?
3. **理解分块�?*
   - `TextChunker.java`：文本分�?   - 分块大小和重叠策�?
4. **理解向量�?*
   - `EmbeddingService.java`：向量化接口
   - `OllamaEmbeddingClient.java`：智谱AI实现

5. **理解向量存储**
   - `MilvusVectorStoreService.java`：Milvus操作
   - Collection结构和索�?
6. **调试文档处理**
   - 上传一个小文档�?页PDF�?   - �?`DocumentIngestionConsumer.onMessage()` 打断�?   - 单步跟踪整个处理流程

**阅读文档**：`docs/wiki/06-关键业务流程.md`（流�?�?
---

### 第七步：理解检索机制（1天）

**目标**：知道如何找到相关内�?
1. **理解检索通道**
   - `IntentMilvusSearchChannel.java`：意图导�?   - `KeywordMilvusSearchChannel.java`：关键词
   - `VectorGlobalSearchChannel.java`：全局向量

2. **理解通道选择**
   - `RetrievalEngine.selectChannels()`
   - 根据置信度选择通道

3. **理解并行检�?*
   - `CompletableFuture` 并行执行
   - 专用线程�?`multiChannelRetrievalExecutor`

4. **理解后处�?*
   - `DeduplicationPostProcessor.java`：去�?   - `RerankPostProcessor.java`：重排序

5. **实验不同检索策�?*
   ```java
   // 修改通道优先�?   @Override
   public int priority() {
       return 1;  // 改为2试试
   }
   
   // 修改Top-K
   int topK = 10;  // �?改为10
   ```

6. **查看检索结�?*
   - �?`RetrievalEngine.retrieve()` 打断�?   - 观察每个通道返回的结�?   - 观察去重和重排后的结�?
**阅读文档**：`docs/wiki/03-系统架构说明.md`（多通道检索架构）

---

### 第八步：理解AI抽象层（半天�?
**目标**：知道如何对接不同AI服务

1. **理解接口设计**
   - `LLMService.java`：对话接�?   - `EmbeddingService.java`：向量化接口

2. **理解实现�?*
   - `OllamaChatClient.java`：智谱AI对话
   - `OllamaChatClient.java`：Ollama对话

3. **理解路由�?*
   - `RoutingLLMService.java`：多模型路由
   - 优先级和故障转移

4. **理解熔断机制**
   - `ModelSelector.java`：模型选择�?   - 连续失败3次后熔断

5. **实验切换模型**
   ```yaml
   # 启用Ollama
   ai:
     chat:
       candidates:
         - id: qwen2.5-ollama
           enabled: true
           priority: 0  # 改为最高优先级
   ```

**阅读文档**：`docs/wiki/05-核心模块详解.md`（AI抽象模块�?
---

### 第九步：理解基础设施（半天）

**目标**：知道框架层提供了什�?
1. **理解上下文管�?*
   - `UserContext.java`：用户上下文
   - `TransmittableThreadLocal`：跨线程传�?
2. **理解链路追踪**
   - `@RagTraceNode`：追踪注�?   - `RagTraceContext.java`：追踪上下文

3. **理解限流机制**
   - `@ChatRateLimit`：限流注�?   - `ChatRateLimitAspect.java`：AOP实现

4. **理解幂等控制**
   - `@IdempotentSubmit`：幂等注�?   - Redis实现原理

5. **查看追踪数据**
   ```bash
   # 发起对话后，查询追踪
   curl http://localhost:9090/api/knowflow/rag/trace/{taskId}
   ```

**阅读文档**：`docs/wiki/05-核心模块详解.md`（基础设施模块�?
---

## 实战练习�?-7天）

### 练习1：新增一个检索通道�?天）

**任务**：实现一个基于时间的检索通道，优先返回最新的内容

**步骤**�?1. 创建 `TimeBasedSearchChannel.java`
2. 实现 `SearchChannel` 接口
3. 设置优先级为 `3`
4. �?`search()` 方法中添加时间过�?5. 测试效果

**参考代�?*�?```java
@Component
public class TimeBasedSearchChannel implements SearchChannel {
    
    @Override
    public int priority() {
        return 3;
    }
    
    @Override
    public List<Chunk> search(List<SubQuestionIntent> intents, int topK) {
        String filter = String.format("create_time > '%s'", 
            LocalDateTime.now().minusDays(7));  // 最�?�?        
        return milvusVectorStore.search(
            knowledgeBaseId,
            queryVector,
            topK,
            filter
        );
    }
}
```

---

### 练习2：实现一个新的后处理器（半天�?
**任务**：实现一个过滤器，移除相似度低于阈值的分块

**步骤**�?1. 创建 `ScoreFilterPostProcessor.java`
2. 实现 `PostProcessor` 接口
3. 设置执行顺序
4. 过滤低分分块
5. 测试效果

**参考代�?*�?```java
@Component
public class ScoreFilterPostProcessor implements PostProcessor {
    
    @Override
    public int order() {
        return 3;  // 在去重和重排之后
    }
    
    @Override
    public List<Chunk> process(List<Chunk> chunks) {
        double threshold = 0.7;  // 阈�?        return chunks.stream()
            .filter(chunk -> chunk.getScore() >= threshold)
            .toList();
    }
}
```

---

### 练习3：优化会话记忆（1天）

**任务**：实现会话记忆的持久化，避免TTL过期丢失

**步骤**�?1. 创建数据库表 `t_conversation_history`
2. 创建 `ConversationHistoryMapper.java`
3. 修改 `ConversationMemoryService`
4. Redis作为缓存，数据库作为持久�?5. 测试效果

**参考代�?*�?```sql
CREATE TABLE t_conversation_history (
    id VARCHAR(64) PRIMARY KEY,
    conversation_id VARCHAR(64) NOT NULL,
    role VARCHAR(20) NOT NULL,
    content TEXT NOT NULL,
    create_time TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_conversation_id (conversation_id)
);
```

---

### 练习4：实现Prompt版本管理�?天）

**任务**：将Prompt模板存储到数据库，支持版本管�?
**步骤**�?1. 创建�?`t_prompt_template`
2. 创建 `PromptTemplateService`
3. 实现版本管理和灰度发�?4. 修改 `RAGPromptService` 使用数据库模�?5. 测试效果

---

### 练习5：实现API性能监控�?天）

**任务**：集成Micrometer，监控接口性能

**步骤**�?1. 添加依赖 `micrometer-registry-prometheus`
2. 配置Prometheus端点
3. 添加 `@Timed` 注解
4. 启动Prometheus和Grafana
5. 创建监控面板

---

## 进阶学习（持续）

### 阅读顺序建议

**第一�?*�?1. �?项目概览
2. �?目录结构解读
3. �?系统架构说明
4. �?启动流程
5. �?核心模块详解

**第二�?*�?6. �?关键业务流程
7. �?关键数据结构
8. �?配置与环�?9. �?外部依赖

**第三�?*�?10. �?隐式规则与约�?11. �?风险点与技术�?12. �?新人上手路线（本篇）

### 推荐学习资源

**RAG相关**�?- 论文：Retrieval-Augmented Generation for Knowledge-Intensive NLP Tasks
- 博客：LangChain RAG教程
- 视频：吴恩达 RAG 课程

**向量数据�?*�?- Milvus官方文档
- PostgreSQL 8 使用说明

**Spring Boot**�?- Spring Boot官方文档
- MyBatis-Plus文档

**分布式系�?*�?- Redisson分布式锁
- RocketMQ消息队列

### 调试技�?
1. **日志调试**
   ```yaml
   logging:
     level:
       com.nageoffer.ai.knowflow: debug  # 开启DEBUG日志
   ```

2. **断点调试**
   - 在关键方法打断点
   - 观察变量�?   - 单步执行

3. **远程调试**
   ```bash
   java -agentlib:jdwp=transport=dt_socket,server=y,suspend=n,address=5005 -jar knowflow.jar
   ```

4. **性能分析**
   - JProfiler
   - VisualVM
   - Arthas

### 常见问题排查

**问题1：对话没有返回结�?*
- 检查知识库是否有数�?- 检查向量检索是否成�?- 检查LLM API是否正常

**问题2：文档处理失�?*
- 查看RocketMQ消费者日�?- 检查Tika是否支持该文件格�?- 检查Embedding API是否正常

**问题3：检索结果不相关**
- 调整Top-K�?- 检查向量化模型
- 优化Prompt

**问题4：性能�?*
- 检查数据库索引
- 检查线程池配置
- 检查网络延�?
### 贡献代码

1. **Fork项目**
2. **创建分支**
   ```bash
   git checkout -b feature/my-feature
   ```
3. **提交代码**
   ```bash
   git commit -m "feat: 新增XXX功能"
   ```
4. **推送分�?*
   ```bash
   git push origin feature/my-feature
   ```
5. **创建Pull Request**

### 联系方式

- GitHub Issues：报告Bug或提�?- 邮件：项目维护者邮�?- 微信群：扫码加入开发者群

---

## 最小理解路�?
如果时间有限，只想快速理解核心逻辑，按以下路径�?
**30分钟快速理�?*�?1. 阅读 `README.md`
2. 查看 `RAGChatServiceImpl.streamChat()`
3. 理解10个步骤的作用
4. 跑通一个对话API

**2小时深入理解**�?1. 阅读 `docs/wiki/01-项目概览.md`
2. 阅读 `docs/wiki/06-关键业务流程.md`（流�?�?3. 调试一次完整对话流�?4. 查看数据库和Redis数据

**1天全面理�?*�?1. 阅读所有Wiki文档
2. 调试对话流程和文档处理流�?3. 理解数据模型和存储结�?4. 完成练习1（新增检索通道�?
---

## 学习检查清�?
完成以下检查项，说明你已经掌握了项目核心：

**基础理解**�?- [ ] 能独立搭建开发环�?- [ ] 能跑通对话API
- [ ] 能上传文档并查询
- [ ] 理解项目目录结构

**核心流程**�?- [ ] 能画出对话流程图
- [ ] 能解释每个环节的作用
- [ ] 理解数据如何存储
- [ ] 理解向量检索原�?
**代码能力**�?- [ ] 能新增一个检索通道
- [ ] 能新增一个后处理�?- [ ] 能修改Prompt模板
- [ ] 能调试定位问�?
**架构理解**�?- [ ] 理解模块划分原则
- [ ] 理解依赖关系
- [ ] 理解设计模式应用
- [ ] 能提出优化建�?
**生产能力**�?- [ ] 能部署到生产环境
- [ ] 能配置监控告�?- [ ] 能排查线上问�?- [ ] 能进行性能优化

