?# 配置与环�?
## 1. 配置文件列表

### 1.1 核心配置文件

| 文件�?| 路径 | 用�?| 是否提交Git |
|--------|------|------|------------|
| `application.yaml` | `bootstrap/src/main/resources/` | 主配置文�?| �?�?|
| `application-dev.yaml` | `bootstrap/src/main/resources/` | 开发环境配�?| �?�?|
| `application-prod.yaml` | `bootstrap/src/main/resources/` | 生产环境配置 | �?�?|
| `ragent-secrets.yaml` | 项目根目�?| 敏感信息（API Key�?| �?�?|
| `docker-compose.yml` | 项目根目�?| 本地开发环�?| �?�?|
| `pom.xml` | 项目根目�?| Maven依赖配置 | �?�?|

### 1.2 数据库脚�?
| 文件�?| 路径 | 用�?|
|--------|------|------|
| `0-schema.sql` | `resources/database/` | PostgreSQL 16 表结�?|
| `1-init_data.sql` | `resources/database/` | 初始化数�?|
| `upgrade_v1.1_to_v1.2.sql` | `resources/database/` | 版本升级脚本 |
| `upgrade_v1.2_to_v1.3.sql` | `bootstrap/src/main/resources/database/` | 版本升级脚本 |

---

## 2. 环境变量说明

### 2.1 必需环境变量

| 变量�?| 说明 | 默认�?| 示例 |
|--------|------|--------|------|
| `OLLAMA_BASE_URL` | 智谱AI API密钥 | �?| `6fceab98d9814fd2...` |

### 2.2 可选环境变�?
| 变量�?| 说明 | 默认�?| 示例 |
|--------|------|--------|------|
| `SERVER_PORT` | 服务端口 | `9090` | `8080` |
| `OLLAMA_BASE_URL` | Ollama服务地址 | `http://localhost:11434` | `http://192.168.1.100:11434` |
| `OLLAMA_ENABLED` | 是否启用Ollama | `false` | `true` |
| `OLLAMA_CHAT_MODEL` | Ollama对话模型 | `qwen2.5:0.5b` | `llama3:8b` |
| `OLLAMA_EMBEDDING_MODEL` | Ollama向量化模�?| `qwen2.5:0.5b` | `nomic-embed-text` |
| `OLLAMA_EMBEDDING_DIM` | Ollama向量维度 | `896` | `768` |

### 2.3 设置环境变量

**Windows (PowerShell)**�?```powershell
$env:OLLAMA_BASE_URL="your_api_key_here"
$env:OLLAMA_ENABLED="true"
```

**Linux/Mac (Bash)**�?```bash
export OLLAMA_BASE_URL="your_api_key_here"
export OLLAMA_ENABLED="true"
```

**永久设置（推荐）**�?�?`ragent-secrets.yaml` 中配置：
```yaml
ai:
  providers:
    ollama:
      url: http://localhost:11434
```

---

## 3. 不同环境配置差异

### 3.1 开发环境（dev�?
**激活方�?*�?```yaml
spring:
  profiles:
    active: dev
```

**特点**�?- 详细日志输出（`logging.level.root: info`�?- 本地数据库（`localhost:5432`�?- 本地Redis（`localhost:6379`�?- 本地Milvus（`localhost:19530`�?- 允许跨域（CORS�?- 关闭某些安全检�?
**配置示例**�?```yaml
# application-dev.yaml
logging:
  level:
    root: info
    com.nageoffer.ai.ragent: debug

spring:
  datasource:
    url: jdbc:postgresql://127.0.0.1:5432/ragent?client_encoding=UTF8
```

### 3.2 测试环境（test�?
**激活方�?*�?```yaml
spring:
  profiles:
    active: test
```

**特点**�?- 中等日志级别（`logging.level.root: info`�?- 测试数据库（独立实例�?- 模拟外部服务
- 启用性能监控

### 3.3 生产环境（prod�?
**激活方�?*�?```yaml
spring:
  profiles:
    active: prod
```

**特点**�?- 精简日志（`logging.level.root: warn`�?- 生产数据库（高可用集群）
- 生产Redis（哨�?集群模式�?- 生产Milvus（分布式部署�?- 严格安全策略
- 启用监控告警

**配置示例**�?```yaml
# application-prod.yaml
logging:
  level:
    root: warn
    com.nageoffer.ai.ragent: info

spring:
  datasource:
    url: jdbc:postgresql://prod-db-cluster:5432/ragent?client_encoding=UTF8
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5

rag:
  rate-limit:
    global:
      max-concurrent: 10  # 生产环境提高并发
```

---

## 4. 关键配置项详�?
### 4.1 服务器配�?
```yaml
server:
  port: ${RAGENT_SERVER_PORT:${SERVER_PORT:9090}}  # 端口优先级：RAGENT_SERVER_PORT > SERVER_PORT > 9090
  servlet:
    context-path: /api/ragent  # API路径前缀
  tomcat:
    threads:
      max: 16          # 最大工作线程数
      min-spare: 4     # 最小空闲线程数
```

**调优建议**�?- 开发环境：`max: 16`
- 生产环境：`max: 200`（根据CPU核心数调整）

### 4.2 数据库配�?
```yaml
spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    type: com.zaxxer.hikari.HikariDataSource
    username: root
    password: root
    url: jdbc:postgresql://127.0.0.1:5432/ragent?client_encoding=UTF8
    hikari:
      connection-timeout: 5000      # 连接超时（毫秒）
      idle-timeout: 600000          # 空闲超时�?0分钟�?      max-lifetime: 1800000         # 最大生命周期（30分钟�?      maximum-pool-size: 5          # 最大连接数
      minimum-idle: 1               # 最小空闲连接数
      pool-name: RagentHikariPool
```

**常见问题**�?- �?连接超时：检查数据库是否启动，防火墙是否开�?- �?连接池耗尽：增�?`maximum-pool-size`
- �?慢查询：检查索引，优化SQL

### 4.3 Redis配置

```yaml
spring:
  data:
    redis:
      host: 127.0.0.1
      port: 6379
      password: 123456
      database: 0           # 数据库索引（0-15�?      timeout: 3000         # 连接超时（毫秒）
      lettuce:
        pool:
          max-active: 8     # 最大连接数
          max-idle: 8       # 最大空闲连接数
          min-idle: 0       # 最小空闲连接数
```

**用�?*�?- 会话记忆存储（TTL 60分钟�?- 分布式锁
- 分布式信号量
- 限流计数�?
### 4.4 向量数据库配�?
```yaml
rag:
  vector:
    type: milvus
  
  default:
    collection-name: rag_default_store  # Milvus Collection名称
    dimension: 1536                     # 向量维度（必须与Embedding模型一致）
    metric-type: COSINE                 # 相似度度量（COSINE/L2/IP�?
milvus:
  uri: http://localhost:19530
  token: ""                             # 如果启用认证
```

**切换向量数据�?*�?- **Milvus**（推荐）：高性能，支持大规模数据
  ```yaml
  rag.vector.type: milvus
  ```
- 正式运行仅保�?**Milvus** 作为向量检索服�?
### 4.5 RAG核心配置

```yaml
rag:
  query-rewrite:
    enabled: true                   # 是否启用查询改写
    max-history-messages: 4         # 改写时参考的历史消息�?    max-history-chars: 500          # 历史消息最大字符数
  
  rate-limit:
    global:
      enabled: true                 # 是否启用全局限流
      max-concurrent: 1             # 每个用户最大并发数
      max-wait-seconds: 3           # 最大等待时�?      lease-seconds: 30             # 租约时间（防止死锁）
      poll-interval-ms: 200         # 轮询间隔
  
  memory:
    history-keep-turns: 4           # 保留最近N轮对�?    summary-start-turns: 5          # 超过N轮开始总结
    summary-enabled: true           # 是否启用自动总结
    ttl-minutes: 60                 # Redis TTL（分钟）
    summary-max-chars: 200          # 总结最大字符数
    title-max-length: 30            # 标题最大长�?  
  semaphore:
    document-upload:
      name: rag:document:upload
      max-concurrent: 10            # 全局最大并发上传数
      max-wait-seconds: 5
      lease-seconds: 300            # 5分钟租约（文档处理较慢）
  
  knowledge:
    schedule:
      scan-delay-ms: 10000          # 定时扫描间隔�?0秒）
      lock-seconds: 900             # 分布式锁时长�?5分钟�?      batch-size: 20                # 每次扫描处理的文档数
      min-interval-seconds: 60      # 同一文档最小重试间�?```

### 4.6 AI模型配置

```yaml
ai:
  providers:
    ollama:
      url: http://localhost:11434
      url: ${OLLAMA_BASE_URL}
      endpoints:
        chat: /chat/completions
        embedding: /embeddings
    ollama:
      url: ${OLLAMA_BASE_URL:http://localhost:11434}
      endpoints:
        chat: /api/chat
        embedding: /api/embed
  
  chat:
    default-model: qwen2.5-ollama      # 默认对话模型
    deep-thinking-model: qwen2.5-ollama  # 深度思考模�?    candidates:
      - id: qwen2.5-ollama
        provider: ollama
        model: qwen2.5-ollama
        priority: 0                     # 优先级（越小越优先）
        supports-thinking: false
        enabled: true
      - id: qwen2.5-ollama
        provider: ollama
        model: ${OLLAMA_CHAT_MODEL:qwen2.5:0.5b}
        priority: 100                   # 备用模型
        enabled: ${OLLAMA_ENABLED:false}
        supports-thinking: false
  
  embedding:
    default-model: ollama-embedding
    candidates:
      - id: ollama-embedding
        provider: ollama
        model: embedding-3
        dimension: 1536
        priority: 0
        enabled: true
      - id: qwen2.5-ollama-embed
        provider: ollama
        model: ${OLLAMA_EMBEDDING_MODEL:qwen2.5:0.5b}
        dimension: ${OLLAMA_EMBEDDING_DIM:896}
        priority: 100
        enabled: ${OLLAMA_ENABLED:false}
  
  keyword:
    extractor: ollama                   # 关键词提取器
    model-id: qwen2.5-ollama
    max-keywords: 5                     # 每个分块最多提�?个关键词
  
  selection:
    failure-threshold: 3                # 连续失败N次后熔断
    open-duration-ms: 30000             # 熔断恢复时间�?0秒）
  
  stream:
    message-chunk-size: 5               # 流式输出分块大小
```

### 4.7 RocketMQ配置

```yaml
rocketmq:
  name-server: 127.0.0.1:9876
  producer:
    group: ragent-producer_pg
    send-message-timeout: 2000          # 发送超时（毫秒�?    retry-times-when-send-failed: 2     # 同步发送失败重试次�?    retry-times-when-send-async-failed: 2  # 异步发送失败重试次�?  consumer:
    consume-thread-min: 2               # 最小消费线程数
    consume-thread-max: 4               # 最大消费线程数
```

### 4.8 文件存储配置

```yaml
rustfs:
  url: http://localhost:9000
  access-key-id: dummy                  # S3兼容的Access Key
  secret-access-key: dummy              # S3兼容的Secret Key
  bucket-name: ragent                   # 存储桶名�?```

---

## 5. 容易出错的配置项

### 5.1 向量维度不匹�?
**错误现象**�?```
Error: Vector dimension mismatch. Expected 1536, got 896
```

**原因**�?- Embedding模型维度与Milvus Collection维度不一�?
**解决**�?```yaml
# 确保一�?rag.default.dimension: 1536
ai.embedding.candidates[0].dimension: 1536
```

### 5.2 API Key未配�?
**错误现象**�?```
Warn: OLLAMA_BASE_URL not configured
Error: 401 Unauthorized
```

**解决**�?1. 创建 `ragent-secrets.yaml`�?   ```yaml
   ai:
     providers:
       ollama:
         api-key: your_actual_api_key
   ```
2. 或设置环境变量：
   ```bash
   export OLLAMA_BASE_URL="your_actual_api_key"
   ```

### 5.3 Redis密码错误

**错误现象**�?```
Error: WRONGPASS invalid username-password pair
```

**解决**�?```yaml
spring.data.redis.password: 123456  # 确保与docker-compose.yml中一�?```

### 5.4 Milvus连接失败

**错误现象**�?```
Error: Failed to connect to Milvus at localhost:19530
```

**解决方案1**：启动Milvus
```bash
docker-compose up -d milvus-standalone
```

**解决方案2**：检�?Milvus 连接参数并确认服务健�?```yaml
rag.vector.type: milvus
milvus.uri: http://localhost:19530
```

### 5.5 端口冲突

**错误现象**�?```
Error: Port 9090 is already in use
```

**解决**�?```yaml
server.port: 8080  # 修改为其他端�?```

或停止占用端口的进程�?```bash
# Windows
netstat -ano | findstr :9090
taskkill /PID <PID> /F

# Linux/Mac
lsof -i :9090
kill -9 <PID>
```

### 5.6 数据库表不存�?
**错误现象**�?```
Error: relation "t_knowledge_base" does not exist
```

**解决**�?```bash
# 执行数据库初始化脚本
psql -h 127.0.0.1 -U postgres -d ragent -f backend/resources/database/schema_pg.sql
psql -h 127.0.0.1 -U postgres -d ragent -f backend/resources/database/init_data_pg.sql
```

### 5.7 线程池队列满

**错误现象**�?```
Error: Task rejected from ThreadPoolExecutor
```

**解决**�?```java
// ThreadPoolExecutorConfig.java
new LinkedBlockingQueue<>(100)  // 增加队列大小�?00
```

### 5.8 会话记忆TTL过短

**现象**：用户反�?对话记录丢失"

**解决**�?```yaml
rag.memory.ttl-minutes: 120  # �?0分钟增加�?20分钟
```

---

## 6. 配置优先�?
从高到低�?1. **命令行参�?*：`--server.port=8080`
2. **环境变量**：`SERVER_PORT=8080`
3. **ragent-secrets.yaml**：敏感信�?4. **application-{profile}.yaml**：环境特定配�?5. **application.yaml**：默认配�?
**示例**�?```bash
# 最终端口为8080（命令行优先级最高）
export SERVER_PORT=9090
java -jar ragent.jar --server.port=8080
```

---

## 7. 配置检查清�?
启动前检查：

- [ ] PostgreSQL 已启动（`docker-compose ps postgres`�?- [ ] Redis 已启动且密码正确
- [ ] Milvus 已启�?- [ ] RocketMQ 已启�?- [ ] RustFS 已启�?- [ ] `OLLAMA_BASE_URL` 已配�?- [ ] 数据库表已创建（执行 `resources/database/schema_pg.sql`，或使用 docker-compose 自动初始化）
- [ ] 端口 9090 未被占用
- [ ] 向量维度配置一致（1536�?
---

## 8. 配置模板

### 8.1 最小配置（本地开发）

**ragent-secrets.yaml**�?```yaml
ai:
  providers:
    ollama:
      url: http://localhost:11434
```

**application.yaml**�?```yaml
spring:
  profiles:
    active: dev

rag:
  vector:
    type: milvus
```

### 8.2 完整配置（生产环境）

**application-prod.yaml**�?```yaml
server:
  port: 9090
  tomcat:
    threads:
      max: 200

spring:
  datasource:
    url: jdbc:postgresql://prod-db:5432/ragent?client_encoding=UTF8
    hikari:
      maximum-pool-size: 20

rag:
  vector:
    type: milvus
  rate-limit:
    global:
      max-concurrent: 10

logging:
  level:
    root: warn
```

