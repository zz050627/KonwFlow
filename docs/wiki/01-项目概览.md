?# KnowFlow 项目概览

## 项目目标

KnowFlow 是一�?*企业�?RAG（检索增强生成）智能体系�?*，提供基于知识库的智能问答能力。核心目标是让企业能够快速构建自己的知识库问答系统，支持文档上传、智能检�?��多轮对话、意图识别等功能�?
## 主要功能

### 核心功能
1. **知识库管�?*：支持创建多个知识库，每个知识库独立管理文档和向量数�?2. **文档处理**：支�?PDF/DOCX/Markdown 等格式，自动解析、分块、向量化
3. **智能问答**：基�?RAG 架构，结合向量检索和大模型生成答�?4. **多轮对话**：支持上下文记忆，自动总结历史对话
5. **意图识别**：树状多级意图分类（领域→类别→主题�?6. **混合检�?*：语义检�?+ 关键词检�?+ 意图导向检�?7. **MCP 工具调用**：支持通过 Model Context Protocol 调用外部工具
8. **流式输出**：SSE 实时流式返回，提升用户体�?
### 高级特�?- **查询改写**：多轮对话上下文补全 + 复杂查询拆分
- **歧义引导**：低置信度意图触发澄清问�?- **模型路由**：多模型候选，自动故障转移
- **分布式限�?*：Redis Lua 脚本实现全局并发控制
- **链路追踪**：全流程 trace 记录，可查询每个环节耗时
- **本地部署**：支�?Ollama 本地模型，完全离线运�?
## 适用场景

- **企业知识库问�?*：内部文档、规章制度、技术文档智能检�?- **客服机器�?*：基于产品手册、FAQ 的自动问�?- **技术支�?*：代码库、API 文档的智能查�?- **教育培训**：课程资料、学习材料的智能助手
- **法律咨询**：法律条文、案例库的检索问�?
## 技术栈

### 后端核心
- **Java 17** + **Spring Boot 3.5.7**
- **MyBatis-Plus 3.5.14**：数据访问层
- **PostgreSQL**：关系数�?+ pgVector 向量存储（可选）
- **Milvus 2.6.6**：主向量数据�?- **Redis**：限流、信号量、会话记�?TTL
- **RocketMQ**：异步文档处理队�?- **Apache Tika 3.2.3**：文档解�?- **Sa-Token 1.43.0**：认证鉴�?- **Redisson 4.0.0**：分布式�?- **Transmittable-ThreadLocal 2.14.5**：线程上下文传�?
### AI 能力
- **智谱 AI（GLM-4.6v-flashx�?*：默认对话模�?- **Ollama**：本地模型支持（qwen2.5:0.5b�?- **自研 LLM 路由�?*：多模型故障转移
- **自研 Embedding 抽象**：支持多种向量化服务

### 前端
- **React 18** + **TypeScript**
- **Ant Design**：UI 组件�?- **Vite**：构建工�?
### 基础设施
- **RustFS**：S3 兼容对象存储
- **Docker Compose**：本地开发环�?- **Maven**：构建工�?- **Spotless**：代码格式化

## 运行方式概览

### 快速启�?```bash
# 1. 启动基础设施
docker-compose up -d

# 2. 构建项目
mvn clean package -DskipTests

# 3. 运行后端
java -jar bootstrap/target/knowflow-0.0.1-SNAPSHOT.jar

# 4. 运行前端（可选）
cd frontend && npm install && npm run dev
```

### 访问地址
- **后端 API**：http://localhost:9090/api/knowflow
- **前端界面**：http://localhost:5173（如果启动了前端�?
### 配置要点
- `application.yaml`：主配置文件
- `knowflow-secrets.yaml`：API Key 等敏感信息（不提�?Git�?- 环境变量：`OLLAMA_BASE_URL`、`OLLAMA_ENABLED` �?
## 项目特色

1. **模块化设�?*：framework（基础设施）、infra-ai（AI 抽象）、bootstrap（业务逻辑）、mcp-server（工具执行）清晰分离
2. **生产级质�?*：分布式锁、限流、幂等、链路追踪、异常处理完�?3. **高并发优�?*�? 个专用线程池，TTL 上下文传递，异步处理
4. **可观测性强**：全链路 trace、详细日志、性能指标
5. **易于扩展**：插件化意图识别、多通道检�?��后处理器链
6. **本地化支�?*：可完全离线运行（Ollama + pgVector�?
