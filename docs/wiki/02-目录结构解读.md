?# 目录结构解读

## 项目根目�?
```
knowflow/
├── bootstrap/          【核心】业务逻辑主模块，包含所有领域代�?├── framework/          【核心】基础设施层：异常、上下文、AOP、ID生成
├── infra-ai/           【核心】AI抽象层：LLM/Embedding客户端、路由、关键词提取
├── mcp-server/         【一般】MCP工具执行服务�?├── frontend/           【核心】React前端项目
├── docs/               【一般】文档目�?├── resources/          【核心】数据库脚本、格式化配置
├── scripts/            【一般】运维脚�?├── docker-compose.yml  【核心】本地开发环境定�?├── pom.xml             【核心】Maven父项目配�?├── CLAUDE.md           【核心】AI助手项目指南
└── README.md           【核心】项目说明文�?```

## 核心模块详解

### 1. bootstrap/ - 业务逻辑主模�?
**职责**：所有业务功能的实现，是项目�?大脑"

```
bootstrap/src/main/java/com/nageoffer/ai/knowflow/
├── KnowFlowApplication.java          # Spring Boot启动�?├── rag/                             【核心领域】RAG对话核心
�?  ├── controller/                  # REST API入口
�?  �?  ├── RAGChatController.java   # /rag/v3/chat SSE流式对话
�?  �?  └── RAGSettingsController.java
�?  ├── service/                     # 业务服务�?�?  �?  └── impl/
�?  �?      └── RAGChatServiceImpl.java  # 核心流程编排
�?  ├── core/                        # RAG核心组件
�?  �?  ├── intent/                  # 意图识别
�?  �?  ├── guidance/                # 歧义引导
�?  �?  ├── memory/                  # 会话记忆
�?  �?  ├── prompt/                  # Prompt组装
�?  �?  ├── retrieve/                # 检索引�?�?  �?  �?  ├── channel/             # 多通道检索（意图/关键�?向量�?�?  �?  �?  └── postprocessor/       # 后处理器（去�?重排�?�?  �?  ├── rewrite/                 # 查询改写
�?  �?  └── vector/                  # 向量存储（Milvus/pgVector�?�?  ├── aop/                         # AOP切面（限流、追踪）
�?  ├── config/                      # 配置类（线程池等�?�?  └── dto/                         # 数据传输对象
�?├── knowledge/                       【核心领域】知识库管理
�?  ├── controller/                  # 知识�?文档/分块CRUD API
�?  ├── service/                     # 业务逻辑
�?  ├── dao/                         # 数据访问�?�?  �?  ├── entity/                  # DO实体
�?  �?  └── mapper/                  # MyBatis Mapper
�?  └── scheduler/                   # 定时任务（文档扫描、超时恢复）
�?├── ingestion/                       【核心领域】文档摄取处�?�?  ├── consumer/                    # RocketMQ消费�?�?  ├── parser/                      # 文档解析器（PDF/DOCX/Markdown�?�?  ├── chunker/                     # 文本分块�?�?  └── util/                        # 工具�?�?├── user/                            【一般领域】用户管�?�?  ├── controller/                  # 登录/注册API
�?  ├── service/                     # 用户业务逻辑
�?  └── config/                      # 用户上下文拦截器
�?├── admin/                           【一般领域】管理后�?�?  └── controller/                  # 系统管理API
�?└── core/                            【基础组件�?    ├── chunk/                       # 分块数据结构
    └── file/                        # 文件元数据管�?```

**关键文件**�?- `RAGChatServiceImpl.java`：核心流程编排（记忆→改写→意图→检索→生成�?- `IntentResolver.java`：意图识别引�?- `RetrievalEngine.java`：多通道检索协调器
- `MilvusVectorStoreService.java`：向量存储操�?- `KnowledgeDocumentServiceImpl.java`：文档生命周期管�?
### 2. framework/ - 基础设施�?
**职责**：跨领域的技术基础设施，被所有业务模块依�?
```
framework/src/main/java/com/nageoffer/ai/knowflow/framework/
├── context/                         # 上下文管�?�?  ├── UserContext.java             # 用户上下文（ThreadLocal�?�?  └── UserContextHolder.java
├── convention/                      # 通用约定
�?  ├── ChatMessage.java             # 对话消息标准格式
�?  ├── ChatRequest.java             # 对话请求
�?  └── Result.java                  # 统一响应格式
├── exception/                       # 异常体系
�?  ├── AbstractException.java       # 异常基类
�?  ├── ClientException.java         # 客户端异常（4xx�?�?  └── ServiceException.java        # 服务端异常（5xx�?├── trace/                           # 链路追踪
�?  ├── RagTraceContext.java         # Trace上下�?�?  └── annotation/
�?      └── RagTraceNode.java        # 追踪节点注解
├── idempotent/                      # 幂等�?�?  └── annotation/
�?      └── IdempotentSubmit.java    # 幂等提交注解
├── redis/                           # Redis工具
�?  └── RedisKeyUtil.java            # Key生成工具
└── snowflake/                       # 雪花ID
    └── SnowflakeIdGenerator.java
```

**关键设计**�?- `UserContext`：使�?TTL（Transmittable-ThreadLocal）实现跨线程上下文传�?- `@RagTraceNode`：AOP记录每个环节的耗时和输入输�?- `@IdempotentSubmit`：基于Redis的分布式幂等控制

### 3. infra-ai/ - AI抽象�?
**职责**：屏蔽不同AI服务商的差异，提供统一接口

```
infra-ai/src/main/java/com/nageoffer/ai/knowflow/infra/
├── chat/                            # 对话能力
�?  ├── LLMService.java              # 统一对话接口
�?  ├── RoutingLLMService.java       # 路由层（多模型故障转移）
�?  ├── StreamCallback.java          # 流式回调接口
�?  └── impl/
�?      ├── OllamaChatClient.java   # 智谱AI实现
�?      └── OllamaChatClient.java    # Ollama本地模型实现
├── embedding/                       # 向量化能�?�?  ├── EmbeddingService.java        # 统一Embedding接口
�?  └── impl/
�?      ├── OllamaEmbeddingClient.java
�?      └── OllamaEmbeddingClient.java
├── keyword/                         # 关键词提�?�?  └── KeywordExtractor.java        # 基于Ollama的关键词提取
├── config/                          # AI配置
�?  ├── AIProviderConfig.java        # 服务商配�?�?  └── ModelCandidateConfig.java    # 模型候选配�?└── selection/                       # 模型选择
    └── ModelSelector.java           # 模型选择器（优先�?熔断�?```

**关键设计**�?- **路由�?*：`RoutingLLMService` 按优先级尝试候选模型，首包探测实现无感切换
- **熔断机制**：连续失败达阈值后暂时跳过该模�?- **流式处理**：统一 `StreamCallback` 接口，支�?SSE/NDJSON 等协�?
### 4. mcp-server/ - MCP工具服务�?
**职责**：实�?Model Context Protocol，让LLM能调用外部工�?
```
mcp-server/src/main/java/com/nageoffer/ai/knowflow/mcp/
├── MCPServerApplication.java        # 独立启动�?├── protocol/                        # MCP协议实现
├── tools/                           # 工具定义
└── executor/                        # 工具执行�?```

**说明**：可独立部署，也可嵌入主应用

### 5. frontend/ - 前端项目

**职责**：用户交互界�?
```
frontend/
├── src/
�?  ├── components/                  # React组件
�?  �?  ├── ChatArea/                # 对话区域
�?  �?  ├── Sidebar/                 # 侧边栏（知识库列表）
�?  �?  └── FunctionPanel/           # 功能面板（上�?设置�?�?  ├── services/                    # API调用
�?  ├── hooks/                       # 自定义Hooks
�?  └── utils/                       # 工具函数
├── public/                          # 静态资�?└── package.json                     # 依赖配置
```

## 可忽略目�?
- `.mvn/`、`.idea/`、`.vscode/`：IDE配置
- `docker-data/`：Docker挂载数据（本地生成）
- `*.log`：运行日志文�?- `.rocketmqlogs/`：RocketMQ日志

## 目录职责总结

| 目录 | 职责 | 重要�?| 依赖关系 |
|------|------|--------|----------|
| bootstrap | 业务逻辑 | ⭐⭐⭐⭐�?| 依赖 framework + infra-ai |
| framework | 基础设施 | ⭐⭐⭐⭐�?| 无依�?|
| infra-ai | AI抽象 | ⭐⭐⭐⭐�?| 依赖 framework |
| mcp-server | 工具执行 | ⭐⭐�?| 依赖 framework |
| frontend | 用户界面 | ⭐⭐⭐⭐ | 调用 bootstrap API |
| resources | 数据库脚�?| ⭐⭐⭐⭐ | 无依�?|
| docs | 文档 | ⭐⭐�?| 无依�?|
| scripts | 运维脚本 | ⭐⭐ | 无依�?|

